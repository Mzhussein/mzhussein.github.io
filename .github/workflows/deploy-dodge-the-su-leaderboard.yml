name: Deploy Dodge The Su Leaderboard Worker to Cloudflare

on:
  push:
    branches: [main]
    paths:
      - 'dodge-the-su/backend/**'
      - '.github/workflows/deploy-dodge-the-su-leaderboard.yml'
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          workingDirectory: dodge-the-su/backend
          command: deploy
