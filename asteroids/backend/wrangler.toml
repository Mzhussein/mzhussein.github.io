name = "asteroids-leaderboard"
main = "worker.js"
compatibility_date = "2024-11-01"

# KV namespace "asteroids_leaderboard" - see README.md in this folder for
# how to create it and get the real ID below. This placeholder ID must be
# replaced before this Worker can deploy successfully.
[[kv_namespaces]]
binding = "LEADERBOARD"
id = "REPLACE_WITH_REAL_KV_NAMESPACE_ID"
