name = "asteroids-leaderboard"
main = "worker.js"
compatibility_date = "2024-11-01"

# KV namespace "asteroids_leaderboard", created via `wrangler kv namespace
# create` (see README.md in this folder for how it was created).
[[kv_namespaces]]
binding = "LEADERBOARD"
id = "f2fb009567894fe08b7c99bf87924032"
